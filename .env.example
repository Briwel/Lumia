# Database Migration Configuration (PostgreSQL)
# Use DATABASE_URL for a connection string (recommended for Cloud / Heroku / Render)
DATABASE_URL=postgres://username:password@localhost:5432/lumina_immo

# Or use individual connection parameters
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=lumina_immo
DB_SSL=false
